# Install the necessary packages
install.packages("dplyr")
install.packages("ggplot2")
install.packages("sf")

# You can also use a single install.packages call with a vector of package names for efficiency
install.packages(c("dplyr", "ggplot2", "sf"))
